# 20 - 專案全盤分析（2026-02-14）

## 1. 當前結論

- 專案已完成 `M0`、`M1`、`M2`、`M3-01~03`。
- 目前最關鍵功能缺口是 `M3-04`（課程樹 MVP 串接）。
- 架構方向正確，但「規格完整度」與「可驗證度」仍有兩個阻塞：
- Unity batchmode 授權問題，導致完整 EditMode 測試無法在此環境執行。
- Meta 課程樹尚未落地，局外主循環仍未閉環。

## 2. 里程碑盤點

| 里程碑 | 狀態 | 說明 |
|---|---|---|
| M0 專案骨架 | Done | Unity 專案骨架、asmdef、config 驗證入口已完成 |
| M1 核心迴圈 | Done | 牌型判定、分數公式、盲注流程、商店流程可運作 |
| M2 學習迴圈 | Done | Lv0~Lv4、答錯三選一、退化、Boss 規則已落地 |
| M3-01~03 | Done | XP/LP、契約、LP 45% 上限已落地 |
| M3-04 | Todo | `TryUnlockNode` 尚未實作 |
| M4 | Todo | 內容平衡尚未開始 |
| Alpha Gate | Todo | 未進回歸與效能驗收 |

## 3. 架構檢查（SA 觀點）

### 3.1 已對齊

- 模組邊界明確：`Run/Scoring/Learning/Shop/Meta/Decay` 皆有責任區分。
- Domain 契約採 `ServiceResult<T>`，錯誤碼一致。
- 測試方向正確：先單元測核心規則，再往整合推進。

### 3.2 未對齊/待補

1. `MetaManagerV2.TryUnlockNode` 尚未實作（M3-04 核心缺口）。
2. 契約生成規則與文件目標有落差：
- 文件定義含類型權重（40/40/20）與分層策略。
- 現況是固定池隨機抽樣，未明確落實權重控制。
3. 商店課程卡出現策略仍是簡化版，未完整符合「Boss 後 2 選 1」規格。
4. 存檔與 migration 流程尚未進入可驗證實作。

## 4. 測試與品質檢查（QA 觀點）

- 已有多份 EditMode 測試檔，覆蓋 M1~M3 關鍵規則。
- `scripts/validate_configs.sh` 可通過。
- 主要阻塞：
- Unity Licensing IPC 問題，導致 batchmode 測試無法在此環境直接驗證。
- 結論：目前可視為「設計正確、程式可讀、待 CI/授權環境驗證」。

## 5. 下一輪實作順序（建議）

1. `M3-04`：實作課程樹解鎖（前置檢查、互斥檢查、LP 扣除、結果回寫）。
2. M3 整合：Meta 結算 + 契約 + 課程樹形成完整局外閉環。
3. `M4-01`：先補可玩內容（T1/T2）供平衡回歸。
4. Alpha 前置：在可授權環境跑完整 EditMode，建立回歸基線。

## 6. 派工建議（可直接分配）

| 任務 | 建議角色 | 交付 |
|---|---|---|
| M3-04 課程樹核心 | 系統工程師 | `TryUnlockNode` + 課程樹資料結構 + 測試 |
| 契約權重規則落地 | 遊戲系統工程師 | 40/40/20 權重抽樣 + 分層測試 |
| 商店課程卡規格對齊 | Gameplay 工程師 | Boss 後 2 選 1 流程與測試 |
| CI 測試通路 | DevOps/工具工程師 | 可授權環境下 EditMode pipeline |

## 7. 驗收門檻（下一階段）

- `M3` 完整 Done 的必要條件：
- `TryUnlockNode` 完成且有互斥/前置測試。
- 契約權重規則對齊文件。
- `docs/17`、`docs/18`、`docs/IMPLEMENTATION_STATUS.md` 同步更新。
