# S10 - UI/UX（正式可玩版重規劃）

## 1. 設計規劃
- 目標：把現有 Prototype UI 從「開發調參工具」升級為「玩家可玩的正式介面骨架」。
- 方向：參考小丑牌（Balatro）資訊密度與操作節奏，但保留本專案學習答題流程。
- 必要原則：
1. 不顯示開發者調參按鈕給玩家。
2. 核心流程一屏可完成：抽牌 -> 選牌/上桌 -> 答題 -> 出牌 -> 結算盲注 -> 商店。
3. 除學習內容外，介面文案維持繁體中文。

## 2. 規格文件
- UI 區塊（正式版 v1）
1. `Top HUD`：關卡、目標分、目前分、剩餘出牌、金錢。
2. `Hand Row`：5 張手牌，可點擊/拖曳上桌，顯示選取狀態。
3. `Play Row`：已上桌牌列，可撤回、可重排（至少保留撤回）。
4. `Action Row`：`開始答題並出牌`、`結算盲注`、`前往下一關`。
5. `Shop Row`：商品卡清單與購買操作。
- 禁止區塊（正式版）
1. 調參面板（難度切換、籌碼加減、模型切換、自動驗證按鈕）。
2. 開發演示按鈕（連跑3局、一鍵跑到通關、失敗後重開演示）。
- 驗收門檻（round-3）
1. 玩家介面中無調參/開發按鈕。
2. 三個核心操作可用：上桌、答題出牌、商店購買。
3. `S10` 專屬測試全綠，且有 MCP job 證據。

## 3. 實作紀錄
- round-1/2 既有成果保留：
1. 非學習區繁中文案。
2. `PrototypeUiText` 共用映射。
3. 舊版 Prototype 控制器自動停用保護。
- round-3 新任務（待實作）：
1. 玩家模式 UI 版型切換（預設玩家模式，不顯示調參區）。
2. 操作流程重排，讓牌列操作成為主焦點。
3. 補 `S10_M6+` 測試覆蓋「正式版不含調參按鈕」與核心互動。

## 4. 驗測報告與調整建議
- 現況結論（2026-02-18）：
1. 現有版本通過「繁中與防回歸」驗測，但仍屬開發導向 UI，不滿足正式可玩目標。
2. 因需求升級，S10 已重新開啟為 `In Progress`。
- 已有證據：
1. `75c0159540894a0884ea2be76bef96cd`（3/3 passed）
2. `ba39f0135508427daf8f1ec0f4301c64`（4/4 passed）
3. `ad4c0f324b8c47b4ae08b2954fcc6a86`（5/5 passed）
- round-3 調整建議（執行順序）：
1. 先做「玩家模式隱藏開發按鈕」與版面重排。
2. 再做牌列互動收斂（上桌/撤回/排序反饋）。
3. 最後補測試與文件回填，再由你決策是否 Done。

## 5. 更新紀錄
- 2026-02-18：round-1 繁中基線完成。
- 2026-02-18：round-2 共用字串映射與舊控制器停用保護完成。
- 2026-02-18：依新需求（正式可玩 UI）重規劃 S10，狀態改回 `In Progress`。
